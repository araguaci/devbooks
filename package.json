{
  "name": "devbooks",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "cd dashboard/client && npm ci && npm run build",
    "dev": "cd dashboard && npm run dev",
    "deploy": "vercel --prod",
    "seed": "cd dashboard/server && npm run seed"
  },
  "dependencies": {
    "@libsql/client": "^0.14.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "jsonwebtoken": "^9.0.2"
  }
}
