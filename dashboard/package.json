{
  "name": "devbooks-dashboard",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,orange \"npm run dev:server\" \"npm run dev:client\"",
    "dev:fresh": "npm run dev:kill && npm run dev",
    "dev:kill": "npx --yes kill-port 3001 5173",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "setup": "cd server && npm install && npm run seed && cd ../client && npm install",
    "deploy": "cd .. && vercel --prod"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
